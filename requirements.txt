# Web server
fastapi>=0.115.0
uvicorn>=0.32.0

# Slack (WebClient for posting messages)
slack-sdk>=3.33.0

# HTTP (PyPI package downloads in main.py)
requests>=2.32.0

# Environment
python-dotenv>=1.0.0

# Testing
pytest>=8.0.0

# Integration framework deps (optional — only needed to run the stubs locally)
# crewai-tools>=0.17.0
# langchain-core>=0.3.0
# llama-index-core>=0.11.0
